// public/firebase-messaging-sw.js
// Minimal fallback — the main PWA service worker (sw.js) handles
// Firebase Cloud Messaging via worker/index.js + importScripts.
// This file exists only so Firebase SDK doesn't complain about
// a missing firebase-messaging-sw.js at the default path.

// No-op: all FCM logic lives in sw.js (generated by next-pwa).
